import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.rana',
  appName: 'rana',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
